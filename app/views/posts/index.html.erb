<div class="container">
  <div class="posts">
  <% @posts.each do |post| %>
    <div class="post">
      <h2>
        <%= post.title %>
      </h2>
      <p>
        <%= post.body %>
      </p>
      <div class="actions">
        <%= render partial: "like_button", locals: { post: post } %>
        <%= render partial: "comment_button", locals: { post: post } %>
      </div>
    </div>
  <% end %>
  </div>
</div>